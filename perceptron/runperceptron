import numpy as np
from arff import Arff
from perceptron import PerceptronClassifier


filepath="linsep2nonorigin.arff"
af = Arff()
af.load_arff(filepath)
alldata = af.data
alllength = len(alldata)
doshuffle=True
if doshuffle:
    np.random.shuffle(alldata)
split=int(round(alllength*.7))
trainingset= alldata[:split]
testset=alldata[split:]
percept=PerceptronClassifier()
#percept.initialize_weights(trainingset[:, :2])
percept.fit(trainingset[:, :2], trainingset[:, -1])

